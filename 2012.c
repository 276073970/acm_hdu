#include <stdio.h>
#include <stdbool.h>

int main(void)
{
	int x, y;

	while(1)
	{
		scanf("%d %d", &x, &y);
		if(x==0 && y==0)
			break;
		else
		{
			
		}
	}

	return 0;
}

bool Prime(int n)
{
	if(n==1)
		return false;
}
